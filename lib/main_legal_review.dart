import 'package:flutter/material.dart';

import 'src/legal/legal_review_page.dart';

// Optional review entrypoint. The ordinary app entrypoints remain unchanged.
void main() {
  runApp(const MaterialApp(home: LegalReviewPage()));
}
